# Pinned API-only deps for CI (REP-006). Install after fall_risk_pipeline/requirements-lock.txt.
# Shared ML stack versions come from the pipeline lockfile.
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.1.0
slowapi==0.1.9
