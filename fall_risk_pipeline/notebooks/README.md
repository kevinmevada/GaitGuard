# Exploratory notebooks (optional)

Jupyter notebooks are not part of the production pipeline. The canonical workflow is:

```bash
python run_local.py
```

To explore data interactively, create notebooks here. They are gitignored (`*.ipynb`).
